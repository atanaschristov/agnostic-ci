import { STRINGS as EN_STRINGS } from './en';
import { STRINGS_DOMAIN, SUPPORTED_LANGUAGES } from '../constants';

export const STRINGS = {
	[SUPPORTED_LANGUAGES.EN]: {
		[STRINGS_DOMAIN]: EN_STRINGS,
	},
};
